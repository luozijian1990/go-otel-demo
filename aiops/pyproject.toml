[project]
name = "go-otel-aiops"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi==0.115.8", "pydantic==2.10.6", "httpx==0.28.1", "uvicorn==0.34.0"]
[project.optional-dependencies]
test = ["pytest>=8,<10"]
[tool.pytest.ini_options]
pythonpath = ["."]
