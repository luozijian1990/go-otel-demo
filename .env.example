# scripts/init-demo-env.sh generates a random local signing key.
DEMO_FAULT_SECRET=
# No model API key: diagnosis runs in your AI session using the Skills.
# Explicit external dependency overrides (old config.yaml files are not loaded):
# BUSINESS_MYSQL_DSN=demo:your-password@tcp(your-mysql:3306)/otel_demo?parseTime=true&timeout=3s&readTimeout=10s&writeTimeout=5s
# BUSINESS_REDIS_ADDR=your-redis:6379
# BUSINESS_REDIS_PASSWORD=
